# The frontend build (tsc + vite) needs devDependencies (@types/*, vite,
# typescript, @vitejs/plugin-react). Render sets NODE_ENV=production, which
# makes `npm install` skip devDependencies by default. Force them on so the
# build always has its tools regardless of how install is invoked.
include=dev
